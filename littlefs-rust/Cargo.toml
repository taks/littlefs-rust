[package]
name = "littlefs-rust"
version = "0.1.0"
edition = "2021"
rust-version = "1.87"
description = "Safe Rust API for the LittleFS embedded filesystem"
license = "BSD-3-Clause"
repository = "https://github.com/light-player/littlefs-rust"
readme = "README.md"
keywords = ["littlefs", "filesystem", "embedded", "no-std", "flash"]
categories = ["embedded", "filesystem", "no-std"]

[lib]
name = "littlefs_rust"
path = "src/lib.rs"

[dependencies]
littlefs-rust-core = { version = "0.1", path = "../littlefs-rust-core", default-features = false}
bitflags = "2"
hybrid-array = "0.4"
typenum = "1.20"

[dev-dependencies]
env_logger = "0.10"

[features]
default = ["std"]
alloc = ["littlefs-rust-core/alloc"]
std = ["alloc"]
log = ["littlefs-rust-core/log"]
